{
  "name": "ispy",
  "version": "1.0.0",
  "description": "React+Vite frontend with TypeScript and TailwindCSS, FastAPI backend with Gemini AI integration",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python3 main.py",
    "build": "cd frontend && npm run build",
    "install:all": "npm install --prefix frontend && pip3 install -r backend/requirements.txt",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "author": "",
  "license": "MIT"
}